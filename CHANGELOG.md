# 更新日志

本文件记录行业动态网页各版本的新增能力、重要调整和待验证变化。

版本记录遵循以下规则：

- 已发布版本记录实际交付结果，不写尚未实现的规划；
- 下一版本规划统一放在“待发布”区；
- 内容更新与网页能力更新分开记录；
- 修复事实错误时说明影响范围，不静默覆盖；
- 日期使用北京时间，格式为 `YYYY-MM-DD`；
- 只有 GitHub Actions 成功、Pages 部署完成且线上冒烟通过后，才记录为“已发布”；已完成但尚未上线的改动记录为“已完成、未上线”，不填写尚未发生的发布日期。

## V1.7（待发布，2026-09-15）

本轮为「存量章节 vs 日更章节」分层落地、第八章公开口径收敛，以及公开面残留清理。全部改动**仅在工作区完成、尚未推送上线**。

### 内容分层（存量 vs 日更）

- 新增内部原始源 `internal/技术能力清单.md`（四列表格：能力名称／能力内容（版本/演进）／效果与优劣势／服务场景；版本状态统一为 当前／已废弃／在研），该目录已列入 `.gitignore`，**永不进入公开仓库**；
- 三层关系：`internal` 原始源（不公开，手动维护）→ `content/行业动态追踪.md` 第八章整理版（公开，稳定）→ 第四至七章日更内容（公开，每日可能更新）；
- `docs/CONTENT_SCHEMA.md` 新增 §9、`AGENTS.md` 新增「存量章节与日更章节」一节。

### 第八章公开口径收敛（安全调整）

- 内部资源标识与参数脱敏：音色 ID 群（4 处）、私有协议名（1 处）、客户名与资源 ID（1 处），以及同类内部参数名与资源模型标识全部改为中性表述；
- 状态口径：章内状态标记统一收敛为「存量材料」，章首统一标记改为「状态：存量材料」；
- 版本状态统一为 当前／已废弃／在研（`（当前最新）`／`（Hot）`／`（上新）` → `（当前）`）；
- 内部材料名改为「客户需求调研」；`## 在研方向` 四条研发方向保留；
- 第三方 IP 角色名改为通用角色类型（角色配音／方言／风格化、卡通角色／故事角色等）；
- 价格数字改为「按调用量计费」（原为按年、按语种计价的具体金额）。

### 前七章口径调整（定位＝行业动态追踪）

- 前七章 11 处「与现有能力/技术的关系及跟进判断」自述改为中性的「对标参照与跟进判断」，去除自身能力盘点与第一人称口吻；
- 「与业务的关系」一节的自有指标自述改为第三人称行业视角，去掉自有语种数与准确率等指标口径；
- `docs/CONTENT_SCHEMA.md`／`AGENTS.md`／`docs/ACCEPTANCE_CHECKLIST.md` 的状态标记口径由旧标记统一为「存量材料」。

### 依赖同步与公开面收敛

- `tests/governance.test.mjs`、`tests/content-sync.test.mjs`、`app/trend-explorer.tsx` 的状态断言与硬编码标签同步；
- `CHANGELOG.md`／`docs/PROJECT_HANDOFF.md` 中残留的内部端点串、内部协议名、客户名、内部材料名一并脱敏；
- `tests/content-sync.test.mjs` 的反向断言样本清单移出公开仓库（改读 `.gitignore` 覆盖的 `internal/forbidden-public-tokens.txt`，缺失时退化为通用结构断言）；
- `package.json` 包名去掉组织特征（改为 `ai-frontier-tracker`）——**仅元数据**，不影响 Pages 构建产物路径（`vite build.base` 为相对路径、`configure-pages` 不读取包名）；
- `README.md`／`AGENTS.md` 增加「**禁止在本仓库执行 `git stash`**」硬纪律（Windows + PortableGit 下被中断会损坏 `.git` 元数据且难以恢复）。

### 公开面通用化（第十七轮追加，2026-09-15）

- 第八章存量区去伙伴标识：两节第三方图像识别集成的**伙伴名**改为通用描述（某电商图像识别／某 OCR 服务商），覆盖能力总览表格行、两节标题与优劣描述；两节本就位于第八章（存量章节），不需移动；
- 章内平台名去标识：自有语音／认知大模型**平台名**改为通用表述；删除一条与内部大模型联动的说明（正文中名为 `DGX Spark` 的公开产品名保留）；
- 存量效果指标改为通用描述：具体识别率／响应时延／提升幅度数字，以及「领先／标杆／与人工高度一致」类表述，一律改为「较高／较快／提升明显／口径对齐」；**覆盖范围类描述保留**（如语种覆盖数）；
- 内部组织名去标识：团队／部门名与内部研究机构称谓改为「平台团队」「内部研发团队」（内容源 10 处 + 网页文案 1 处 + 文档 13 处）。**例外并保留**：正文中三家国家标准公开编制机构的名称；
- 内部原始源改名为 `internal/技术能力清单.md`（原文件名含组织名），并同步 `AGENTS.md`／`CHANGELOG.md`／`docs/CONTENT_SCHEMA.md`／`docs/PROJECT_HANDOFF.md` 的引用；
- 迁移说明：`README.md` 与 `docs/CONTENT_SCHEMA.md` §10 记录「旧仓库归档、Pages 地址变更、旧链接失效」；
- 回归令牌清单扩为**五类**（新增公司名、组织名、包名与第三方伙伴名等 **7 个**样本），写入 `.gitignore` 覆盖的 `internal/forbidden-public-tokens.txt`。

### 验证

- `sync-content` → `node --test` **9/9 通过** → `eslint` → `tsc --noEmit` → `vite build` 全部通过；
- 公开面残留扫描按**五类清单**（内部资源标识／接口端点／客户与伙伴／指标与商务／**公司名、组织名、包名**）执行，命中数均为 0。

### 总览标记去第一人称

- 总览段标记更名为 `【团队优先级与行动判断】`（由第一人称改为团队视角），同步 `content/行业动态追踪.md`、`docs/CONTENT_SCHEMA.md` §3、`docs/PROJECT_HANDOFF.md` §14.8。

### 仍未处理（待决策）

- git 历史层仍保留旧文本 —— 按「新建干净仓库（`ai-frontier-tracker-public`）+ 旧仓库 Archive」方案解决，迁移步骤见工作区 `01-运行机制/待确认事项.md`；
- 尚未推送：本机到 GitHub 两条通路（系统代理／直连）均实测失败，恢复后先推旧仓库止血，再执行迁移。


## V1.6（2026-09-15）

本次发布合并 2026-09-15 的日更内容轮次、第八章复原与「三、重点摘要」更名、以及常驻左栏与分类筛选等页面能力更新。GitHub Actions **run #12** 成功，Pages 部署时间 `2026-09-15T02:47:55Z`（提交 `1a414ae`，与 V1.5 的 `46c917f` 之间无中间推送）。

上线前门禁全绿：`sync-content`（8 sections / 19 highlights）→ `node --test` **9/9 通过** → `eslint` → `tsc --noEmit` → `vite build`；上线后以线上产物实测复核（见「上线实测」）。

### 内容更新（2026-09-15）

- **新增 7 条详细条目 + 7 行重点摘要**（`content/行业动态追踪.md`）：
  - 「四、行业动态」新建 `## 2026-09-14—2026-09-20` 周期，收录 2 条：商务部等八部门《促进智能家居消费行动方案》（部委文件首次把“方言识别”写入适老化设计口径，并将制定智能家居互联互通强制性国家标准）、GB/T 47746—2026《顾客联络服务 人工与智能客户服务协同要求》（“人工入口不得隐藏”“人机切换不得重复询问”成为可检查口径）；
  - 「六、技术革新」新建 `## 2026-09-14—2026-09-20` 周期，收录 2 条：SteerDuplex／SteerBench（全双工可操控性被系统评测，合成停顿误打断 26.5% → 9%）、Duplex Cue（补上“轮内适应”第三种正确反应）；
  - 「七、竞品与标杆公司动态」`2026-09-14—2026-09-20` 周期追加 2 条（豆包手机助手消费者版与 SAEP 屏幕自动化声明协议、Apple Watch Audio Intelligence 的同意与窃听法争议），`2026-09-07—2026-09-13` 周期前置 1 条（鸿蒙版腾讯视频首发端侧 A2A）；
  - 「三、重点摘要」在 `2026-09-14—2026-09-20` 表尾追加 6 行、`2026-09-07—2026-09-13` 表尾追加 1 行；
- **未收录（退回补证）**：中国广电一体化电视适配器全国集采——集采公告与技术附件原文未取得，媒体口径不作为事实源，待一手公告到手后转正式收录；
- **本轮未动「二、总览」**：一周内 Apple（助手代用户跨应用操作）、字节（应用声明授权边界）、华为（应用注册 Agent、系统只做路由）三种“系统助手 ↔ 第三方应用”协作范式已成型，但证据尚需稳定，另行单独走一轮总览更新；
- **归属待确认**：Apple Watch Audio Intelligence 条目暂归「七、竞品与标杆公司动态」（主体为 Apple 产品动作）；若按隐私／法律议题定位，可平移至「四、行业动态」，正文不变；
- **影响范围**：本内容轮次为**纯插入**（该轮事实源 0 行删除、144 行新增）；锚点无重复，重点摘要引用全部可解析，`归因` 字段全部落在允许枚举内，CRLF 保持一致；
- **门禁**：`sync-content`（8 sections / 19 highlights）→ `node --test` **9/9 通过** → `eslint` → `tsc --noEmit` → `vite build` 全部通过（`dist/assets/index-Dae2mChs.js` 847.88 kB、`index-B5iKioX9.css` 26.91 kB，属已知非阻断大包警告）；
- **未处理（待决策，独立问题）**：git 历史中仍保留第八章原文与脱敏前的内部端点表述，属仓库历史层敞口，需单独处理。

### 内容结构调整（2026-09-15）

- **复原「八、公司内部进展」整章**（`content/行业动态追踪.md`）：按 `b5062db^` 逐字取回 557 行（`**能力总览**` 表、16 个 `###` 能力域、45 个 `####` 能力卡片与 `## 在研方向`），并在「二、总览」收束段后补回 `→ 对应章节：八、公司内部进展` 归属标记；一级章节由七个回到八个，`sync-content` 切分为 **8 sections**；
- **「三、本期重点摘要」更名为「三、重点摘要」**：标题与正文自指各 1 处；`scripts/sync-content.mjs`、`tests/governance.test.mjs`、`tests/content-sync.test.mjs`、`docs/CONTENT_SCHEMA.md`、`docs/END_TO_END_CONTRACT.md`、`docs/ACCEPTANCE_CHECKLIST.md`、`docs/WEEKLY_PUBLISH_RUNBOOK.md`、`AGENTS.md`、`README.md`、`app/trend-explorer.tsx` 同步；
- **依赖同步**：`tests/governance.test.mjs` 的 `requiredSections` 补回「八、公司内部进展」并新增该章四条存在性断言；`tests/content-sync.test.mjs` 断言回到 8 sections（`numeral:"八"`、`id:"section-8"`、`title:"重点摘要"`）；`app/trend-explorer.tsx` 复原 `InternalProgressReport`／`parseInternalProgress` 与 `SectionBody` 分支；`app/globals.css` 复原 19 条 `.internal-*` 规则（花括号配平 270/270）；
- **安全影响（负责人已确认并接受）**：复原后 V1.5 关闭的第八章 P0 敞口**重新打开**——音色标识（4 处）、内部协议名（1）、客户名（1）、状态标记（17）等内部表述再次进入公开内容源与线上产物；A 类三项脱敏（`接口地址从略`×5、`已知性能边界`×1）仍保留在章内。本次决策为「复原并公开推送」。

### 页面能力更新（2026-09-15）

- **新增常驻左侧导航栏（`site-rail`）**：`app/trend-explorer.tsx` 新增 `railTargets` 与 `railSection`，用 `IntersectionObserver` 跟随滚动高亮当前章节；左栏含「板块导航」5 项、章节导航 8 章、以及「分类筛选」5 个下拉；宽视口 `position: sticky` 常驻，窄视口（≤860px）以 `display: contents` 退化为普通区块、章节标题吸顶；
- **新增分类筛选**：按终端／能力／竞对／归因／层级 5 个维度筛选章节内条目；筛选生效时自动展开对应分组（`filterDetailsRef`）；
- **删除首屏四个数据卡片**：移除 `metric-grid`／`Metric` 组件与 `.metric-grid`／`.metric-card` 样式，保留标题区与「浏览全部章节」入口；
- **无头 Edge 实机核对 16/16 通过**：顶部卡片数为 0、左栏常驻 3 区块、章节导航 8 章、板块锚点 5 个、筛选下拉 5 个、宽／窄视口均无横向溢出、下滚左栏仍吸附、第八章渲染（groups=2／categories=16／cards=45）、窄视口左栏转普通区块且导航换行不裁切。

### 上线实测（2026-09-15）

- 线上首页 `https://fengyunnnn.github.io/ai-frontier-tracker/` 的 `index.html` 已指向新产物 `assets/index-5SHDGMU6.js`（847,882 字节）与 `assets/index-B5iKioX9.css`；
- 线上产物内容核对：`公司内部进展`×3、`能力总览`×1、`在研方向`×3、`section-8`×82（第八章已复原）；`本期重点摘要`=0、`重点摘要`×5（更名已生效）；`metric-grid`=0（首屏卡片已删）；`site-rail`×1、`rail-link`×3、`rail-filters`×1（左栏与筛选已上线）；本轮新增条目锚点 `smart-home-consumption-plan`／`steerduplex`／`doubao-mobile-assistant-saep`／`tencent-video-harmonyos-a2a` 各 ×6；
- 第八章复原后内部串重新出现（音色标识×4、内部协议名×1、客户名×1、状态标记×21），与「复原并公开推送」的决策一致。
## V1.5（2026-09-15）

本次发布合并了 2026-09-14 的页面层修复、内容调整与「第八章移出公开内容源」三组改动。GitHub Actions **run #10** 成功，Pages 部署时间 `2026-09-15T01:53:30Z`（提交 `b5062db`，与 V1.4 的 `24bf403` 之间无中间推送）。

上线前门禁全绿：`sync-content`（7 sections / 12 highlights）→ `node --test` **9/9 通过** → `eslint` → `tsc --noEmit` → `vite build`。上线后以线上产物实测复核（见「安全调整」末条）。

本轮之前的内容轮次已随 V1.4 发布，不再重复记录：第一轮（八章标题去状态后缀、跨周条目归自然周、补齐情报维度）、第四轮（文档说明重构 + 窄视口导航修复）、第六轮（总览四层重构）、第十轮（内部接口／错误码／压测三项脱敏）。

### 页面层修复（2026-09-14）

- **修复正文渲染被标题注入截断的问题**：章节标题原先被替换成裸 HTML 标签以挂内容锚点，裸标签会开启 CommonMark 的 HTML block 并把标题之后直到空行为止的内容整段吞成纯文本。同一处 bug 导致三个现象——「文档目的／服务对象／更新方式」的分点不换行、「总览」整章挤成一坨、「三、本期重点摘要」的表格显示成一片竖线原文。现改为剥掉源码里显式的 `{#anchor}` 标记后让 `marked` 正常解析标题、再按文档顺序回填锚点；
- 「本期值得优先关注」在板块顶部新增周期切换 Tab，一次只渲染该周重点，取代原先 8 个日期组纵向堆叠；
- 修复后补齐正文表格样式（列宽上限、斑马纹、表头主题色、按需横向滚动）。

### 内容调整（2026-09-14）

- 「一、文档说明」删除两段：「同一事件可同时进入两条轨道…」（对双轨制的二次解释）与「信息源按固定源、专项源和线索源三类管理…」（属 `docs/INFORMATION_SOURCES_AND_CONTENT_DIRECTION.md` 的职责，正文只需指路）；
- 「一、文档说明」的文档目的、服务对象、更新方式：引导句与分点之间补空行，使分点在页面各占一行；文案未改；
- 「二、总览」在不改动任何文字的前提下重排为「一行一块」并给四条编号判断的说明行加 3 空格缩进，使 `【能力层】`／`【产品层】`／`【产业层】`／`【我们自己｜优先级与行动判断】` 与正文在页面上呈现为分层结构，四条编号判断仍为同一个有序列表；页面「竞争判断分层」卡片的数据源不变。

### 安全调整（2026-09-14）

- **第八章「公司内部进展」整章移出公开内容源**：删除 `content/行业动态追踪.md` 中原 L1527–2083 共 557 行（`**能力总览**` 表、16 个 `###` 能力域、45 个 `####` 能力卡片与 `## 在研方向`），并删掉总览中 `→ 对应章节：八、公司内部进展` 的归属标记；一级章节由八个收敛为七个；
- 依赖同步（8 处）：`docs/CONTENT_SCHEMA.md` §2 章节清单与 §7（由「公司内部进展」改写为「内部内容边界」）、`docs/END_TO_END_CONTRACT.md`、`docs/ACCEPTANCE_CHECKLIST.md`、`docs/WEEKLY_PUBLISH_RUNBOOK.md`、`AGENTS.md` 内容边界、`tests/governance.test.mjs` 的 `requiredSections` 与断言、`tests/content-sync.test.mjs` 的反向断言、`app/trend-explorer.tsx` 中的内部进展专用组件；
- 随后失效的 19 条 `.internal-*` 样式规则从 `app/globals.css` 删除（本轮自查发现，无任何引用）；
- 影响说明：该章自 2026-09-03 首次 Pages 部署起即可被公开访问。本次连同 V1.4 的 A 类三项最小脱敏一起，关闭此前登记的全部 P0 敞口；
- **线上实测（2026-09-15T01:57Z）**：线上产物为 `index-CfVY0Qmg.js`（714,175 字节）+ `index-DzuDsUE1.css`（24,536 字节），V1.4 的线上包 `index-CYO5JoOO.js` 已返回 404；三个产物中 `公司内部进展`、内部端点串、内部错误码、内部协议名、音色标识、客户名、`能力总览`、`内部进展` 命中数**均为 0**；`numeral:"八"` 与 `八、` 命中数均为 0，章节数为七个；本次改动标记 `overview-layer`／`overview-ref`／`highlight-tabs`／`markdown-overview` 在线上产物中均已出现；
- 仍未处理（待决策）：其他章节中的「与现有能力的关系」自述共 13 处（含若干未核验的自有能力指标与版本代号罗列）、`L1227` 引用的内部材料名与 `L1228` 的「待核验」标记。这些属分析内容而非章节结构，改动会改变报告判断表述，不在本次授权范围内。

## V1.4（2026-09-14）

本次发布合并了 2026-09-14 的两次部署：

- 第 1 次：`26ef278..2426cd9`，GitHub Actions run #7 成功，Pages 部署时间 `2026-09-14T08:14:04Z`；
- 第 2 次：`2426cd9..5defb20`，GitHub Actions run #8 成功，Pages 部署时间 `2026-09-14T09:55:07Z`。

两次均经线上实测冒烟（八章导航完整、窄视口目录可见、内容标记与脱敏标记核对通过）后记录为已发布。

### 内容调整（2026-09-14）

- 将四—七章的跨周条目按各自证据的发布时间归入自然周（周一至周日），原集中在章末“持续观察”的 13 条不再单独成区；对应提交 `2a856e3`；
- 为四—七章共 22 条缺失情报维度的条目补齐终端、能力、归因和竞争层级；第四至第七章的维度覆盖由 19 条提升至 41 条（分章为 9、10、10、12）；对应提交 `84cbc98`；
- 移除“公司内部进展”二级标题上的状态后缀（“存量材料”“待核验”），并删除与章首“内容边界”“阅读说明”重复的“待补充的跟踪字段”小节；对应提交 `d03863e`；
- “文档说明”新增“内容覆盖”小节，明确报告主线为语音交互与 AI 人机交互，说明轨道 A（交互体验与 AI 前沿战略雷达）与轨道 B（业务落地与产业约束雷达）的关系，并将双轨制说明从“更新方式”中移出；对应提交 `2d6b0eb`；
- 重写“总览”引言与四个 Level 段落，按“决定能否上牌桌 / 用户是否愿意持续使用 / 长期位置”分层定位，把运营商采购与验收口径收回到 Level 1，避免全篇读成验收导向；对应提交 `2d6b0eb`；
- 为“四、行业动态”“六、技术革新”“八、公司内部进展”三个一级章节标题补前置空行，消除标题与上一段正文粘连的渲染隐患；对应提交 `2d6b0eb`；
- 将“总览”重写为四层全景（能力层—产品层—产业层—我们自己），覆盖技术前沿、产品动态、产业约束与内部准备，修正此前以运营商准入为主轴、与网页实际追踪范围不匹配的问题；对应提交 `506b730`；
- 收敛“总览”措辞：四个编号判断（Level 1／Level 2／Level 3／横向诊断轴）归入统一的“【我们自己｜优先级与行动判断】”块，明确其为面向外部变化的优先级与投入判断，不是对已确认内部能力现状的陈述；开头层面数由四改为三；四个编号条目逐字保留，页面“竞争判断分层”卡片的数据源不变；对应提交 `7f97a9a`。

### 安全脱敏（2026-09-14）

- 移除内容源中的内部接口地址、内部错误码与内部压力测试细节：5 处接口地址替换为占位说明、1 行错误码整行删除、1 处压测描述改写为“已知性能边界”；对应提交 `86f8b56`；
- 上述三类内容自 2026-09-03 首次 Pages 部署起即可被公开访问，本次为确认后的止血性清理；线上构建产物实测相关标记命中数为 0；
- 同类的内部标识清理仍待确认后另行发布，不随本版本生效。

### 页面层调整

- 修复窄视口（≤860px）下侧栏一级目录被裁剪的问题：原先一级章节排成单行横向滚动条，390px 视口实测内容宽 1114px、可视宽仅 340px，第三至第八章被推出可视区，且因容器 `overflow: hidden` 与外层标题隐藏而没有可见的滚动提示；现改为自动换行排列，八章在 1440/1280/1100/1000/900/820/390 七档宽度下均完整可见；对应提交 `a28ecd5`。此改动取代了下方阶段 1 的“移动端一级目录横向滚动”行为，二级目录仍然隐藏。

### 阶段 1 页面层调整

- 页面由“仪表盘/门户”调整为“资讯流优先”，模块顺序为数据概览 → 本期重点 → 趋势判断 → 全文搜索 → 多维情报筛选 → 完整报告；对应提交 `daf12f6`；
- 重点资讯按具体日期分组，每张卡片仅保留一个语义链接，标签最多显示两个；
- 多维筛选使用原生 `details` 与五个 `select`，默认折叠，仅在筛选生效时展示结果；
- 搜索词在跳转正文后保留；移动端一级目录横向滚动、二级目录隐藏；正文表格仅在自身区域横向滚动。

### 文档与流程资产

- 纳入四份 `docs/` 契约与运行文档（`c03777b`）：端到端契约、周度发布运行手册、验收清单、项目交接文档；
- 将“涉及数字的判断必须实测”“新增测试必须做反向验证”两条写入跨工具协作协议；
- 将“敏感信息三面检查”（工作区／线上站点／git 历史）写入 `AGENTS.md`：只清工作区不等于清干净公开面，提交历史是一份永久的公开记录；对应提交 `5defb20`；
- 将总览“四层叙事”从硬性治理断言降级为文档规范与人工核对项，治理测试只保留 4 条编号判断及其紧邻说明行的数据契约断言（叙事可变、契约必守）；对应提交 `9f4a4fc`。

> 说明：2026-09-14 的 `.git` 仓库从回收站恢复属运维事件，不改变交付内容，不计入本变更日志。

## V1.3（2026-09-09）

本次发布：

- 增加仓库级 `AGENTS.md`，固化内容、视觉、交互、解析和生成产物的职责边界；
- 增加架构说明、Markdown 内容契约和修改发布指南；
- 增加 Pull Request 检查清单；
- 增加维护规则自动测试，并为自动生成数据添加禁止手工编辑声明；
- 将完整报告目录升级为八章信息卡片面板，展示摘要、关键词、小节数和相对内容体量；
- 增加报告摘要面板，以及单章阅读、展开全部和收起全部模式；
- 将全文搜索升级为关键词与能力、产品、厂商、场景、待确认标签组合检索；
- 搜索结果按章节分组，并增加匹配范围速览和二级标题精确定位；
- 根据当前章节原文提取待确认、待验证和建议动作线索。
- 清理从富文本复制产生的外部展示占位符，并重构行业趋势的事件事实、分析、共同信号及业务关系层级；
- 将产品、技术和竞品章节的具体事件统一为四级标题，时间范围继续作为二级导航；
- 为本期重点摘要增加稳定详情锚点，支持卡片和搜索结果精确跳转到正文条目；
- 扩展 Markdown 内容契约与自动测试，阻止典型复制残留重新进入网页。
- 依据内部原始材料重整“公司内部进展”，增加阅读说明和 16 个能力域总览；
- 将内部能力明细改为按能力域分组的折叠卡片，并统一标注“存量材料 · 待核验”。
- 将行业、产品、技术与竞品条目改为结论摘要卡片，详细证据按需展开；
- 增加终端类型、能力域、竞对、问题归因和竞争层级五维组合筛选；
- 建立 L1 生存层、L2 竞争层、L3 未来层以及能力、体验、资源/商务、交付问题归因框架；
- 补充运营商标准、机顶盒芯片和牌照媒资接口三类 OS 平台落地情报；
- 将酷开 AIOS 分析从功能罗列升级为客户决策链和问题归因矩阵；
- 更新竞争情报内容契约、同步解析器和自动治理测试。

发布说明：本节能力由提交 `26ef278` 构建，经 GitHub Actions run #6 成功发布至 GitHub Pages（构建时间 `2026-09-09T09:24:17Z`），线上页面与稳定锚点已验证可访问。

口径修正说明：`26ef278` 提交时，本节内容在 CHANGELOG 中标记为“待发布”；提交 `48c96b9` 将其改为“V1.3（2026-09-09）”，但该提交尚未推送，因此远程仓库的 CHANGELOG 仍显示“待发布”。线上实际已包含本节全部能力。待 `48c96b9` 推送后，远程记录将与实际状态一致。

## V1.2（2026-09-04）

本次发布：

- 增加由 Markdown 二级、三级标题自动生成的章节内导航；
- 将“文档说明”拆分为文档目的、服务对象、更新方式和覆盖时间；
- 将行业、产品、技术及竞品内容按时间范围建立二级分栏；
- 统一技术革新章节的时间顺序，最新周期前置；
- 重构“公司内部进展”为已有能力、能力分类、具体能力、在研方向及待补充字段；
- 为内部存量材料增加“待核验”边界，避免把历史描述直接视为当前事实。

## V1.1（2026-09-04）

目标：提升首页视觉层级、内容可扫读性和长报告阅读效率。

本次发布：

- 重构首页四层信息层级：核心结论、变化主线、本期重点、完整报告；
- 增加本期阅读指南及自动统计；
- 增加重点事件卡片展开/收起；
- 前置全文搜索入口并显示结果数量；
- 增加按月份筛选本期重点事件；
- 增加阅读进度条和返回顶部入口；
- 强化“进入完整报告”的范围说明。

## V1.0（2026-09-03）

首次发布：

- 将最新行业动态 Markdown 转换为 GitHub Pages 静态网页；
- 展示四条变化主线、本期重点事件和完整报告章节；
- 支持全文关键词搜索和搜索结果定位；
- 支持章节导航、稳定锚点和关键词标签；
- 完成桌面端与移动端响应式适配；
- 建立 Markdown 自动同步、内容测试和生产构建流程；
- 建立 GitHub Actions 自动部署流程。

## 趋势状态启用原则

### 基线积累阶段（前3—4期）

只允许使用：

- `本期新增`：当前周期第一次形成、且达到趋势假设门槛；
- `持续跟踪`：此前已建立，本期仍有相关事件或仍需观察。

在没有稳定跨期基线前，不使用“增强”或“降级”，避免把单次事件数量变化误判为趋势强弱变化。

### 跨期判断阶段（至少积累3—4期后）

满足可比时间窗口、分类口径和证据记录后，才可增加：

- `增强`：出现新的独立主体、连续产品化、跨层证据或关键门槛变化；
- `降级`：支持证据中断、关键假设被反证、产品路线收缩或外部条件发生逆转。

“增强/降级”必须写明上一期状态、新增或减少的证据、判断依据和反证条件，不能只根据新闻数量判断。
