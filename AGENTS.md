# AI Frontier Tracker 项目维护规则

本文件适用于整个仓库，用于约束人工维护、AI 辅助修改和后续自动化。详细职责、内容格式与操作流程分别见 `docs/ARCHITECTURE.md`、`docs/CONTENT_SCHEMA.md` 和 `CONTRIBUTING.md`。

## 不可破坏的五条边界

1. 事实内容只写在 `content/行业动态追踪.md` 中，不得把具体事件、日期、来源、行业判断或内部进展写死在页面组件里。
2. 视觉风格只放在 `app/globals.css` 中，优先修改 `:root` 设计变量，再调整具体组件样式。
3. 交互状态和页面结构放在 `app/trend-explorer.tsx` 中；内容组件不得成为事实数据的第二来源。
4. 仅在 Markdown 数据格式、字段或解析契约发生变化时修改 `scripts/sync-content.mjs`，不得用解析脚本修补某一期具体内容。
5. `app/content.generated.ts` 与 `dist/` 是生成产物，禁止手工修改；必须通过同步或构建命令生成。

## 修改路由

- 改“说什么”：修改 Markdown。
- 改“如何识别内容”：先更新内容契约，再修改同步脚本和测试。
- 改“如何排列和操作”：修改 TSX。
- 改“长什么样”：修改 CSS。
- 改“如何发布”：修改 GitHub Actions，并同步更新架构说明和贡献指南。

## 内容与事实边界

- 区分外部事实、分析判断、建议和待确认项。
- 外部事件应保留来源和发布时间；聚合信息尽量追溯一手来源。
- 公司内部能力、版本、指标、价格、接口状态和在研计划必须有当前有效内部材料支持；否则标记“存量材料”。
- 不得因视觉或交互改版改写事实内容，也不得为填充页面虚构字段。

## 存量章节与日更章节

内容分三层，禁止互相覆盖：

| 层 | 载体 | 是否公开 | 更新频率 |
|---|---|---|---|
| raw 原始源 | `internal/技术能力清单.md`（`.gitignore`） | 不公开 | 手动维护 |
| 整理版（存量章节） | `八、公司内部进展` | 公开（脱敏后） | 稳定，不随日更变化 |
| 日更章节 | 第四至第七章 | 公开 | 每日可能更新 |

- 日更只动四至七章，**不得改动第八章**；第八章只在手动更新原始源后重新整理。
- 此前「移除 vs 复原」反复的根因是未区分两类章节；改内容前先判断目标章节属于哪一层。
- 公开面只放脱敏整理版；原始源与未公开标识／参数留在 `.gitignore` 覆盖的 `internal/`。


## 变更要求

- 采用最小必要修改，保留不在当前任务范围内的用户内容和代码。
- 新增数据字段时，按“内容契约 → Markdown → 同步脚本 → 页面展示 → 样式 → 测试”的顺序更新。
- 提交前运行 `pnpm check`；失败时不得发布。
- **每次内容更新必须同步 4 处时间字段**：`## 覆盖时间` 正文、总览首个「截至」日期、重点摘要「本期周期：」、周期标题 `## A—B`。页头「更新于」由内容源文件的 mtime 自动生成，**不得写进内容源**。清单、不变量与两个格式陷阱见 `docs/CONTENT_SCHEMA.md` §4.1；`pnpm check` 会拦截不一致。
- 实际发布后才在 `CHANGELOG.md` 中记录为已发布版本；未发布内容必须明确标记为待发布。
- 未经明确要求，不修改全局 Skill、个人配置或公司正式业务材料。

## 验证纪律

- **涉及数字的判断必须实测**：条目数、锚点数、通过数、领先提交数、某视口下元素是否可见等，一律以运行时实测或脚本统计为准，不得估算或凭目测下结论；报告时注明测量方式与条件。
- **布局与可见性问题必须实机渲染验证**：源码逻辑正确不等于用户看到的是正确的。仅读 CSS/JSX 不足以断言渲染结果。
- **新增测试必须反向验证**：把被测对象改回缺陷状态，确认新测试失败，再还原确认通过；报告中给出反向验证结果，不得只声明“已加测试”。
- **多个症状先假设同一根因**：同一页面同时出现多个异常（如“分点不换行”“整章挤成一坨”“表格显示成一片竖线”）时，先查是不是一处渲染／解析缺陷的共同表现，再决定改几处。追根因通常比分别打补丁更省力，也不易留下下一个症状；一个改动修多处，比三套补丁更易维护。

## 敏感信息三面检查

处理内部、个人或未经确认的信息时，必须**同时**检查三个面。只清其中一面，公开面仍然是脏的。

1. **工作区**：`content/`、`docs/`、`CHANGELOG.md`、`app/`、`tests/` 等当前文件。改动只影响本地，未推送前公网内容不变。
2. **线上站点**：Pages 部署的构建产物对公网可见。`dist/` 虽被 `.gitignore` 排除，但部署结果可被任意抓取；必须等推送触发部署后，以线上产物为准复核。
3. **git 历史**：旧提交永久保留全部历史文本，任何人可 `git log -p` 或检出任一旧版本读取。**删除工作区文件不会移除历史**；提交历史是一份永久的公开记录，写入前先判断"这句话能否公开"。

执行规则：

- 内部资源标识（音色 ID、内部协议名）、错误码、接口地址、资源 ID、客户名与未公开参数只留在 `.gitignore` 覆盖的 `internal/` 原始源；公开内容只放脱敏整理版；
- 脱敏用**精确字符串替换 + 出现次数断言**，不用宽泛模式替换，避免误伤与漏改；
- 抓接口地址必须同时覆盖 `http(s)://` 与 `ws(s)://` 两种形式及域名变体，否则会出现"只改一半"；
- 报告脱敏结果时，必须说明线上是否已生效、历史是否仍可读；
- **令牌清单必须覆盖五类**：内部资源标识／接口端点／客户与伙伴／指标与商务／**公司名、组织名、包名**。漏任一类，「残留＝0」的结论都不成立；
- **变更说明类文档本身也是泄露面**：`CHANGELOG`、`PROJECT_HANDOFF`、评审记录与「自我更正」会复述被脱敏的原文，脱敏后必须用「文档层」清单**再扫一遍**；
- 报告「残留＝0」前，先写明本次清单覆盖了哪几类。

## 公开面操作的授权归属

涉及公开面的任何改动（`content/`、`app/`、`docs/`、`CHANGELOG.md`、`README.md`、`package.json`、`.github/` 等一切会被推送的文件），先判断「这是谁的授权范围」，再决定动不动：

| 内容 | 谁有权授权 | 授权前的默认状态 |
|---|---|---|
| 个人的分析、判断与公开表述 | 本人 | 可照常公开 |
| 涉及所属组织的内部信息（接口、错误码、内部标识、组织名等） | 组织**合规／安全流程** | ⛔ 保持脱敏 |
| 涉及客户或第三方的信息（客户、伙伴、价格、第三方名称等） | **商务／法务流程** | ⛔ 保持脱敏 |

- 授权**逐项**进行，须可追溯「授权人／范围／日期」；
- **未被点到的项维持现状**，不因新请求自动变更；
- **不接受「整体撤销」「全部公开」这类批量表述** —— 逐项核对，才可能知道到底公开了什么；
- 公开面内容有争议时，**默认停在上一次已确认的版本**，不因新请求自动切换。

## 批量编辑纪律

对 `content/`、`docs/` 等长文本做脚本化批量修改时，必须遵守：

- **先备份，再动手**：执行任何批量编辑前，先建一个临时分支或复制源文件，留一份可对照的原始状态（**不得用 `git stash`**，见「禁止事项」）。批量脚本出错时，最可靠的基准是**版本库**（`git diff`、`git show HEAD:<file>`），不是生成产物。
- **生成产物只能作辅助基准**：`app/content.generated.ts` 可作为“改动前原文”的旁证（其 `sections[].body` 是同步时的内容源快照），但它依赖“误操作前刚好同步过一次、且生成物未被误操作破坏”两个前提，**不能作为唯一基准**。
- **批量脚本必须先 dry-run**：先只输出“将要修改的行号 + 上下文”，人工确认命中范围正确后再执行；无法 dry-run 时，先在副本上跑通。
- **脚本必须限定作用区间并断言命中数**：把匹配范围限制在目标章节内，并在脚本里对“实际命中行数”设预期值，命中不等于预期就中止，不落盘。
- **落盘后逐字校验**：改动后重新同步并重切章节，与基准逐字比对一致，再进入门禁。

## 禁止事项

- 不直接编辑 `app/content.generated.ts` 或 `dist/`。
- 不在 CSS 中承载内容事实或业务规则。
- 不在 TSX 中复制整段报告正文。
- 不静默改变 Markdown 的固定一级章节、日期口径或重点摘要字段。
- 不把构建成功等同于内容事实已经核验。
- **不在本仓库执行 `git stash`**：Windows + PortableGit 环境下被中断可能损坏 `.git` 元数据且难以恢复；需要暂存改动时，改用**临时分支**或**复制源文件**（见 `README.md`「日常使用」）。
