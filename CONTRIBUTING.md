# 修改与发布指南

## 1. 修改前先判断类型

| 变更类型 | 首要文件 |
|---|---|
| 事实、日期、来源、判断 | `content/行业动态追踪.md` |
| Markdown 字段或标题规则 | `docs/CONTENT_SCHEMA.md`、`scripts/sync-content.mjs` |
| 页面结构或交互 | `app/trend-explorer.tsx` |
| 视觉和响应式 | `app/globals.css` |
| 发布流程 | `.github/workflows/deploy-pages.yml` |

开始修改前阅读 `AGENTS.md`。涉及内容格式时同时阅读 `docs/CONTENT_SCHEMA.md`；涉及跨层修改时阅读 `docs/ARCHITECTURE.md`。

## 2. 内容更新流程

1. 在 Markdown 的唯一主归属章节中新增或更新条目。
2. 对重点情报补齐“终端、能力、竞对、归因、层级”，并区分能力、体验、资源/商务和交付问题。
3. 检查标题、日期、来源、核验状态和时间周期；具体招采指标、芯片能力和接口权限没有一手证据时必须标记待核验。
4. 竞品条目至少回答一次“客户为什么买单”，不以功能列表代替客户决策链。
5. 避免在重点摘要和详细章节重复维护两份完整正文。
6. 运行 `pnpm check`。
7. 检查生成结果后提交源文件和自动生成数据；不得提交 `dist/`。

## 3. 视觉调整流程

1. 先修改 `app/globals.css` 顶部的统一变量。
2. 只有统一变量无法覆盖需求时，才修改具体组件样式。
3. 不在 CSS 中写入事实内容或业务规则。
4. 检查桌面端、平板和移动端断点。
5. 运行 `pnpm check`。

## 4. 交互调整流程

1. 在 `app/trend-explorer.tsx` 中维护页面结构和状态。
2. 同时覆盖默认状态、空结果、选中状态和必要的异常状态。
3. 保留可访问名称、键盘操作和足够的触控区域。
4. 如果交互依赖新数据字段，先执行“数据格式变更流程”。
5. 运行 `pnpm check`。

## 5. 数据格式变更流程

必须按以下顺序操作：

```text
更新 CONTENT_SCHEMA
        ↓
修改 Markdown 示例和真实内容
        ↓
修改 sync-content.mjs
        ↓
修改 TSX 展示与交互
        ↓
修改 CSS
        ↓
增加自动测试
```

不得先修改生成文件，也不得在同步脚本中写死某一期事件。

## 6. 本地校验

```bash
pnpm check
```

该命令依次执行：

- 内容同步和契约测试；
- 代码规范检查；
- TypeScript 检查和生产构建。

单独调试时可使用：

```bash
pnpm dev
pnpm test
pnpm lint
pnpm build
```

## 7. 提交与发布

1. 检查变更范围，不混入无关文件。
2. 确认 `app/content.generated.ts` 来自同步命令，而非手工编辑。
3. 确认 `dist/` 没有被提交。
4. 更新 `CHANGELOG.md`：未发布内容标记“待发布”；线上部署成功后再记录正式日期。
5. 推送到 `main` 后，由 GitHub Actions 自动构建并发布。
6. 以 Actions 成功且线上页面出现新内容作为发布完成标准。

## 8. 完成标准

一次变更只有同时满足以下条件才算完成：

- 内容或功能符合目标；
- 文件职责没有越界；
- 自动检查全部通过；
- 变更记录与实际交付一致；
- 如要求发布，线上版本已经验证。
